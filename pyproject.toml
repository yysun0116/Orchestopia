[project]
name = "orchestopia"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic-ai>=0.4.3",
]

[dependency-groups]
dev = [
    "ruff>=0.12.7",
]
